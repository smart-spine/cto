---
name: factory-create-agent
description: Create a production-usable OpenClaw sub-agent in an isolated workspace.
---

## GOAL
Create a fully runnable agent for the requested business use-case, not a scaffold.

## REQUIRED INPUTS (INTAKE MUST COMPLETE FIRST)
- agent name and mission,
- target destination/binding,
- behavior and trigger policy,
- failure/retry policy,
- model preference,
- secrets plan (SecretRef only).

If critical inputs are missing, return `BLOCKED: MISSING_CRITICAL_INPUTS`.

## REQUIRED STRUCTURE
Create `workspace-<agent_name>/` with:
- root profile files:
  - `IDENTITY.md`
  - `TOOLS.md`
  - `PROMPTS.md`
  - `AGENTS.md` or `README.md`
- directories:
  - `config/`
  - `tools/`
  - `tests/`
  - `skills/`
- skills minimum:
  - `skills/SKILL_INDEX.md`
  - at least one `skills/<skill-name>/SKILL.md`

Do NOT place base profile files in `workspace-<agent_name>/agent/`.

## CODEX EXECUTION RULES
- Follow `STRICT CODEX DELEGATION PROTOCOL` from `AGENTS.md`.
- Include exact worker requirement: `Write Unit Tests & Verify`.
- Complex logic (`.js/.ts/.py`) MUST be generated by Codex.

## VALIDATION AND SMOKE (MANDATORY)
Before `READY_FOR_APPLY`, MUST run all:
1. Deterministic unit tests.
2. `openclaw config validate --json` if config changed.
3. Functional smoke test for requested business behavior.

Functional smoke MUST prove:
- real input processed,
- expected output produced,
- expected delivery path works when runtime prerequisites exist.

If prerequisites for smoke are missing, return `BLOCKED` with exact missing prerequisite.

## REGISTRATION
Update root `openclaw.json`:
- add/refresh agent entry,
- set `workspace` to `workspace-<agent_name>`,
- keep paths consistent and valid.

## DONE CRITERIA
Return `READY_FOR_APPLY` ONLY if structure, tests, config validation, and pre-apply functional smoke are all green.
